<!DOCTYPE html>
<html>
    <head>
        <meta content="text/html; charset=UTF-8" http-equiv="Content-type" />
    </head>
    <body>
        <h1>You have posted "<%= @post.title %>" and automatically favorited this post.</h1>
        <small>
            By: <%= @post.user.name %>
        </small>
        <p>
            <%= @post.body %>
        </p>
        <p>
            <%= link_to "View post on site", topic_post_url(@post.topic, @post) %>
        </p>
    </body>
</html>